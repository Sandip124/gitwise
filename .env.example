# SQLite database path (default: ~/.gitwise/gitwise.db)
# GITWISE_DB_PATH=~/.gitwise/gitwise.db

# Ollama (local LLM — Phase 2)
OLLAMA_URL=http://localhost:11434
OLLAMA_CHAT_MODEL=llama3
OLLAMA_EMBED_MODEL=nomic-embed-text
