node_modules/
dist/
.env
*.wasm
.DS_Store
coverage/
*.tsbuildinfo

# Auth tokens and secrets — NEVER commit
.mcpregistry_*
.mcp_publisher_token
.npmrc
*.token
*.key
*.pem
*.p12
*.pfx

# Per-developer MCP config (contains local paths)
.mcp.json

# Generated report (can be regenerated)
wisegit-report.html

# wisegit generates CLAUDE.md per-repo — don't commit our own test one
CLAUDE.md
